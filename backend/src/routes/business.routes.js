/**
 * 业务模块路由
 * @category Business
 *
 * 所有业务路由都在 /api/biz 前缀下
 */

const express = require('express');
const router = express.Router();

// 业务模块路由将在这里添加
// 示例：
// const orderRoutes = require('../business/modules/order/order.routes');
// router.use('/orders', orderRoutes);  // 完整路径: /api/biz/orders

// const productRoutes = require('../business/modules/product/product.routes');
// router.use('/products', productRoutes);  // 完整路径: /api/biz/products

module.exports = router;